app = "rg-assistant-bot"
kill_signal = "SIGINT"
kill_timeout = 30

[build]
  dockerfile = "Dockerfile"

[env]
  PYTHONUNBUFFERED = "1"

# No HTTP services needed - this is a bot
# But we need a process to keep the container running
